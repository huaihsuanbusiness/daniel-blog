---
interface Props {
  lang: 'en' | 'zh';
}

const { lang } = Astro.props;

const copy = {
  en: {
    eyebrow: 'Interactive RAG Workbench',
    title: 'This isn’t just an article. It’s a live RAG workbench.',
    description:
      'Ask a real question, watch the runtime choose a policy, and inspect how the system routes, retrieves, evaluates, and answers.',
    statusLoading: 'Checking runtime connection…',
    statusReady: 'Live runtime connected',
    statusError: 'Live runtime needs attention',
    statusHint: 'Connected endpoint',
    questionLabel: 'Ask the RAG a real question',
    questionPlaceholder: 'Try asking when creative mode should be chosen instead of safe mode.',
    exampleLabel: 'Try one of these',
    modeLabel: 'Query mode',
    modeHintAuto: 'Auto lets the runtime policy router decide the cheapest safe path first.',
    modeHintManual: 'Fixed modes are useful when you want a more predictable teaching or debugging path.',
    manualLabel: 'Advanced Runtime controls',
    manualDescription: 'Keep this collapsed for most readers. Expand it only when you want to override the runtime.',
    manualToggle: 'Manual override',
    manualHelper:
      'When manual override is on, these fields are sent explicitly to the API and can replace what auto mode would normally choose.',
    manualSummaryClosed: 'Collapsed by default so the main interaction stays close to the question composer.',
    workflow: 'Workflow rail',
    workflowHelp: 'Route this request through the inspectable workflow rail instead of the direct engine path.',
    toolRouting: 'Tool routing',
    toolRoutingHelp: 'Let the runtime route toward tools or alternate execution paths before answering.',
    retryLoop: 'Retry loop',
    retryLoopHelp: 'Allow reflect-and-retry if the first answer looks weak or mismatched.',
    retrievalMode: 'Retrieval mode',
    retrievalModeHelp: 'Choose between hybrid retrieval and dense-only retrieval.',
    parentExpansion: 'Parent expansion',
    parentExpansionHelp: 'Expand matched child chunks into richer parent context before synthesis.',
    contextCompression: 'Context compression',
    contextCompressionHelp: 'Compress long context before synthesis to save attention budget.',
    longContextReorder: 'Long-context reorder',
    longContextReorderHelp: 'Re-rank long context so the strongest evidence appears earlier.',
    minScore: 'Minimum score',
    minScoreHelp: 'Drop evidence below this retrieval confidence threshold.',
    maxRetries: 'Max retries',
    maxRetriesHelp: 'Cap how many recovery attempts the runtime may spend on this question.',
    hybrid: 'Hybrid',
    dense: 'Dense only',
    askButton: 'Run this request',
    askingButton: 'Running…',
    resetButton: 'Reset',
    answerLabel: 'Final answer',
    routeLabel: 'Request journey',
    routeEmpty: 'The journey ribbon will appear here after the first request finishes.',
    policyLabel: 'Automatic policy decision',
    policyEmpty: 'Run a question to inspect which policy the runtime selected and why.',
    plannerLabel: 'Planner view',
    plannerEmpty: 'The planner panel stays pinned so you can compare before and after each run.',
    runtimeLabel: 'Runtime guardrails',
    runtimeEmpty: 'Guardrails, retry budget, and retrieval controls will be summarized here.',
    timelineLabel: 'Stage trace',
    timelineEmpty: 'Each recorded stage will appear here after the request finishes.',
    traceExpand: 'View full trace',
    traceCollapse: 'Collapse trace',
    sourcesLabel: 'Retrieved sources',
    sourceEmpty: 'No sources returned yet.',
    sourcesExpand: 'View more sources',
    sourcesCollapse: 'Show fewer sources',
    rawLabel: 'Raw response',
    failedPrefix: 'Request failed',
    examples: [
      'When should this runtime choose creative mode instead of safe mode?',
      'Explain the request path from planning to synthesis.',
      'Which parts of this query were handled by policy routing versus retrieval?',
    ],
    emptyAnswer: 'Your answer will appear here after the first run.',
    runningAnswer: 'Calling the live RAG…',
    needsQuestion: 'Please enter a question first.',
    policyLive: 'runtime policy live',
    policyFallback: 'proxy fallback',
    metricsHealth: 'Health',
    metricsPolicy: 'Policy',
    metricsResolvedMode: 'Resolved mode',
    metricsLatency: 'Latency',
    metricsSources: 'Sources',
    connected: 'Connected',
    disconnected: 'Issue detected',
  },
  zh: {
    eyebrow: 'Interactive RAG Workbench',
    title: '這不只是一篇文章，這是一個 live RAG 實驗台。',
    description:
      '你可以直接問一個真實問題，讓 runtime 先選策略，再把 routing、retrieval、evaluation 與回答路徑攤開給你看。',
    statusLoading: '正在確認 runtime 連線…',
    statusReady: 'Live runtime 已連線',
    statusError: 'Live runtime 目前異常',
    statusHint: '目前連到的 endpoint',
    questionLabel: '直接問這個 RAG 一個真實問題',
    questionPlaceholder: '試著問：什麼情況下比較適合 creative mode，而不是 safe mode？',
    exampleLabel: '你可以先試這些問題',
    modeLabel: 'Query mode',
    modeHintAuto: 'Auto 會先交給 runtime policy router 判斷當下最便宜又安全的路徑。',
    modeHintManual: '固定 mode 比較適合教學或除錯，因為你能觀察比較可預期的路徑。',
    manualLabel: '進階 Runtime controls',
    manualDescription: '大多數讀者不需要打開這區。只有想覆寫 runtime 行為時，再展開它。',
    manualToggle: '啟用手動覆寫',
    manualHelper:
      '一旦開啟手動覆寫，這些欄位會被明確送進 API，因此它們可能會取代 auto mode 原本會自動選的設定。',
    manualSummaryClosed: '預設收合，讓主要互動區保持靠近輸入框與送出按鈕。',
    workflow: 'Workflow rail',
    workflowHelp: '改走可觀察的 workflow rail，而不是直接的 engine path。',
    toolRouting: 'Tool routing',
    toolRoutingHelp: '先判斷要不要走工具或其他執行路徑，再開始回答。',
    retryLoop: 'Retry loop',
    retryLoopHelp: '如果第一版答案看起來不夠好，允許系統反思後再試一次。',
    retrievalMode: 'Retrieval mode',
    retrievalModeHelp: '選擇 hybrid 檢索，或只用 dense vector 檢索。',
    parentExpansion: 'Parent expansion',
    parentExpansionHelp: '把命中的 child chunk 展開回更完整的 parent context。',
    contextCompression: 'Context compression',
    contextCompressionHelp: '在 synthesis 前先壓縮長上下文，節省注意力預算。',
    longContextReorder: 'Long-context reorder',
    longContextReorderHelp: '重新排序長上下文，讓最重要的證據更早被模型看到。',
    minScore: 'Minimum score',
    minScoreHelp: '低於這個檢索分數的證據，就不應太信任。',
    maxRetries: 'Max retries',
    maxRetriesHelp: '限制這題最多可以花多少次重試機會。',
    hybrid: 'Hybrid',
    dense: 'Dense only',
    askButton: '送出這次查詢',
    askingButton: '執行中…',
    resetButton: '重設',
    answerLabel: '最終回答',
    routeLabel: '這次請求的旅程',
    routeEmpty: '送出第一個問題後，這裡會顯示這趟請求實際走過的路徑。',
    policyLabel: '自動策略判斷',
    policyEmpty: '送出問題後，這裡會顯示 runtime 最後選了什麼策略，以及為什麼。',
    plannerLabel: 'Planner 視角',
    plannerEmpty: '這個版位會固定保留，方便你對照查詢前後 planner 的差異。',
    runtimeLabel: 'Runtime guardrails',
    runtimeEmpty: '這裡會固定顯示 guardrails、retry budget 與 retrieval controls。',
    timelineLabel: '逐階段 trace',
    timelineEmpty: '每個被記錄的 stage，都會在查詢完成後出現在這裡。',
    traceExpand: '檢視完整 Trace',
    traceCollapse: '收合 Trace',
    sourcesLabel: '這次取回的 sources',
    sourceEmpty: '目前還沒有來源。',
    sourcesExpand: '檢視更多來源',
    sourcesCollapse: '顯示較少來源',
    rawLabel: 'Raw response',
    failedPrefix: '請求失敗',
    examples: [
      '什麼情況下比較適合 creative mode，而不是 safe mode？',
      '幫我解釋這次 request 從 planning 到 synthesis 之間發生了什麼。',
      '這次的 policy routing 與 retrieval 分別做了哪些事？',
    ],
    emptyAnswer: '送出第一個問題之後，回答會出現在這裡。',
    runningAnswer: '正在請求 live RAG…',
    needsQuestion: '請先輸入問題。',
    policyLive: 'runtime policy 已上線',
    policyFallback: '目前仍是 proxy fallback',
    metricsHealth: 'Health',
    metricsPolicy: 'Policy',
    metricsResolvedMode: 'Resolved mode',
    metricsLatency: 'Latency',
    metricsSources: 'Sources',
    connected: '已連線',
    disconnected: '需檢查',
  },
}[lang];
---

<section class="rag-lab" data-rag-lab data-lang={lang}>
  <div class="rag-shell">
    <div class="rag-atmosphere rag-atmosphere-a"></div>
    <div class="rag-atmosphere rag-atmosphere-b"></div>

    <header class="rag-hero">
      <div class="rag-hero-copy">
        <p class="rag-eyebrow">{copy.eyebrow}</p>
        <h2 class="rag-title">{copy.title}</h2>
        <p class="rag-description">{copy.description}</p>
      </div>

      <aside class="rag-runtime-plaque">
        <div class="rag-runtime-head">
          <span class="rag-signal-dot" data-rag-signal></span>
          <div>
            <p class="rag-runtime-label" data-rag-status>{copy.statusLoading}</p>
            <p class="rag-runtime-caption">{copy.statusHint}</p>
          </div>
        </div>
        <p class="rag-runtime-endpoint" data-rag-status-note></p>
      </aside>
    </header>

    <section class="rag-telemetry-ribbon" data-rag-metrics>
      <article class="rag-metric-card">
        <span>{copy.metricsHealth}</span>
        <strong data-rag-metric-health>OK</strong>
      </article>
      <article class="rag-metric-card">
        <span>{copy.metricsPolicy}</span>
        <strong data-rag-metric-policy>{copy.policyFallback}</strong>
      </article>
      <article class="rag-metric-card">
        <span>{copy.metricsResolvedMode}</span>
        <strong data-rag-metric-mode>Auto</strong>
      </article>
      <article class="rag-metric-card">
        <span>{copy.metricsLatency}</span>
        <strong data-rag-metric-latency>—</strong>
      </article>
      <article class="rag-metric-card">
        <span>{copy.metricsSources}</span>
        <strong data-rag-metric-sources>0</strong>
      </article>
    </section>

    <section class="rag-workbench-grid">
      <div class="rag-left-rail">
        <section class="rag-composer-card">
          <div class="rag-composer-head">
            <div>
              <p class="rag-kicker">{copy.questionLabel}</p>
              <h3 class="rag-section-title">{copy.questionLabel}</h3>
            </div>
          </div>

          <label class="rag-field-shell" for="rag-question">
            <textarea id="rag-question" class="rag-question" rows="5" placeholder={copy.questionPlaceholder}></textarea>
          </label>

          <div class="rag-composer-actions">
            <button type="button" class="rag-primary" data-rag-submit>{copy.askButton}</button>
            <button type="button" class="rag-secondary" data-rag-reset>{copy.resetButton}</button>
          </div>

          <div class="rag-example-block">
            <p class="rag-kicker">{copy.exampleLabel}</p>
            <div class="rag-chip-row">
              {copy.examples.map((prompt) => (
                <button type="button" class="rag-chip" data-rag-example={prompt}>{prompt}</button>
              ))}
            </div>
          </div>
        </section>

        <section class="rag-control-rail">
          <div class="rag-mode-card">
            <div class="rag-mode-head">
              <div>
                <p class="rag-kicker">{copy.modeLabel}</p>
                <h3 class="rag-section-title">{copy.modeLabel}</h3>
              </div>
            </div>
            <div class="rag-mode-pills" data-rag-modes>
              <button type="button" class="rag-mode-pill is-active" data-rag-mode="auto">Auto</button>
              <button type="button" class="rag-mode-pill" data-rag-mode="fast">Fast</button>
              <button type="button" class="rag-mode-pill" data-rag-mode="safe">Safe</button>
              <button type="button" class="rag-mode-pill" data-rag-mode="deep_eval">Deep Eval</button>
              <button type="button" class="rag-mode-pill" data-rag-mode="creative">Creative</button>
              <button type="button" class="rag-mode-pill" data-rag-mode="agentic">Agentic</button>
            </div>
            <p class="rag-mode-helper" data-rag-mode-helper>{copy.modeHintAuto}</p>
            <p class="rag-mode-note" data-rag-mode-note hidden></p>
          </div>

          <details class="rag-advanced" data-rag-advanced>
            <summary class="rag-advanced-summary">
              <div>
                <p class="rag-kicker">{copy.manualLabel}</p>
                <h3 class="rag-section-title">{copy.manualLabel}</h3>
              </div>
              <div class="rag-advanced-meta">
                <span class="rag-advanced-preview" data-rag-manual-state>{copy.manualSummaryClosed}</span>
                <span class="rag-advanced-chevron" aria-hidden="true"></span>
              </div>
            </summary>

            <div class="rag-advanced-body">
              <div class="rag-advanced-toggle-row">
                <p class="rag-advanced-copy">{copy.manualDescription}</p>
                <label class="rag-switch rag-switch-master">
                  <input type="checkbox" data-rag-manual-enabled />
                  <span class="rag-switch-track"></span>
                  <span class="rag-switch-label">{copy.manualToggle}</span>
                </label>
              </div>

              <p class="rag-callout" data-rag-manual-helper>{copy.manualDescription}</p>

              <div class="rag-manual-grid" data-rag-manual-fields-wrap>
            <article class="rag-manual-row">
              <div>
                <h4>{copy.workflow}</h4>
                <p>{copy.workflowHelp}</p>
              </div>
              <label class="rag-switch">
                <input type="checkbox" data-rag-field="use_workflow" />
                <span class="rag-switch-track"></span>
              </label>
            </article>

            <article class="rag-manual-row">
              <div>
                <h4>{copy.toolRouting}</h4>
                <p>{copy.toolRoutingHelp}</p>
              </div>
              <label class="rag-switch">
                <input type="checkbox" data-rag-field="use_tool_routing" />
                <span class="rag-switch-track"></span>
              </label>
            </article>

            <article class="rag-manual-row">
              <div>
                <h4>{copy.retryLoop}</h4>
                <p>{copy.retryLoopHelp}</p>
              </div>
              <label class="rag-switch">
                <input type="checkbox" data-rag-field="use_retry_loop" />
                <span class="rag-switch-track"></span>
              </label>
            </article>

            <article class="rag-manual-row">
              <div>
                <h4>{copy.retrievalMode}</h4>
                <p>{copy.retrievalModeHelp}</p>
              </div>
              <select class="rag-select" data-rag-field="retrieval_mode">
                <option value="hybrid">{copy.hybrid}</option>
                <option value="dense">{copy.dense}</option>
              </select>
            </article>

            <article class="rag-manual-row">
              <div>
                <h4>{copy.parentExpansion}</h4>
                <p>{copy.parentExpansionHelp}</p>
              </div>
              <label class="rag-switch">
                <input type="checkbox" data-rag-field="use_parent_expansion" />
                <span class="rag-switch-track"></span>
              </label>
            </article>

            <article class="rag-manual-row">
              <div>
                <h4>{copy.contextCompression}</h4>
                <p>{copy.contextCompressionHelp}</p>
              </div>
              <label class="rag-switch">
                <input type="checkbox" data-rag-field="use_context_compression" />
                <span class="rag-switch-track"></span>
              </label>
            </article>

            <article class="rag-manual-row">
              <div>
                <h4>{copy.longContextReorder}</h4>
                <p>{copy.longContextReorderHelp}</p>
              </div>
              <label class="rag-switch">
                <input type="checkbox" data-rag-field="use_long_context_reorder" />
                <span class="rag-switch-track"></span>
              </label>
            </article>

            <article class="rag-manual-row">
              <div>
                <h4>{copy.minScore}</h4>
                <p>{copy.minScoreHelp}</p>
              </div>
              <input class="rag-number" type="number" min="0" max="1" step="0.01" data-rag-field="min_score" />
            </article>

            <article class="rag-manual-row">
              <div>
                <h4>{copy.maxRetries}</h4>
                <p>{copy.maxRetriesHelp}</p>
              </div>
              <input class="rag-number" type="number" min="0" max="5" step="1" data-rag-field="max_retries" />
            </article>
              </div>
            </div>
          </details>
        </section>
      </div>

      <div class="rag-right-stage">
        <section class="rag-output-grid">
      <section class="rag-panel rag-panel-answer">
        <div class="rag-panel-head">
          <div>
            <p class="rag-kicker">{copy.answerLabel}</p>
            <h3 class="rag-section-title">{copy.answerLabel}</h3>
          </div>
        </div>
        <div class="rag-answer" data-rag-answer>{copy.emptyAnswer}</div>
      </section>

      <section class="rag-panel rag-panel-journey">
        <div class="rag-panel-head">
          <div>
            <p class="rag-kicker">{copy.routeLabel}</p>
            <h3 class="rag-section-title">{copy.routeLabel}</h3>
          </div>
        </div>
        <div class="rag-route" data-rag-route>{copy.routeEmpty}</div>
      </section>

      <section class="rag-panel rag-panel-policy">
        <div class="rag-panel-head">
          <div>
            <p class="rag-kicker">{copy.policyLabel}</p>
            <h3 class="rag-section-title">{copy.policyLabel}</h3>
          </div>
        </div>
        <div class="rag-card-stack" data-rag-policy>{copy.policyEmpty}</div>
      </section>

      <section class="rag-panel rag-panel-runtime">
        <div class="rag-panel-head">
          <div>
            <p class="rag-kicker">{copy.runtimeLabel}</p>
            <h3 class="rag-section-title">{copy.runtimeLabel}</h3>
          </div>
        </div>
        <div class="rag-mini-grid" data-rag-runtime>{copy.runtimeEmpty}</div>
      </section>

      <section class="rag-panel rag-panel-planner">
        <div class="rag-panel-head">
          <div>
            <p class="rag-kicker">{copy.plannerLabel}</p>
            <h3 class="rag-section-title">{copy.plannerLabel}</h3>
          </div>
        </div>
        <div class="rag-mini-grid" data-rag-plan>{copy.plannerEmpty}</div>
      </section>

      <section class="rag-panel rag-panel-trace">
        <div class="rag-panel-head">
          <div>
            <p class="rag-kicker">{copy.timelineLabel}</p>
            <h3 class="rag-section-title">{copy.timelineLabel}</h3>
          </div>
          <button type="button" class="rag-link-button" data-rag-trace-toggle hidden>{copy.traceExpand}</button>
        </div>
        <div class="rag-trace" data-rag-timeline>{copy.timelineEmpty}</div>
      </section>

      <section class="rag-panel rag-panel-sources">
        <div class="rag-panel-head">
          <div>
            <p class="rag-kicker">{copy.sourcesLabel}</p>
            <h3 class="rag-section-title">{copy.sourcesLabel}</h3>
          </div>
          <button type="button" class="rag-link-button" data-rag-sources-toggle hidden>{copy.sourcesExpand}</button>
        </div>
        <div class="rag-sources" data-rag-sources>{copy.sourceEmpty}</div>
      </section>
        </section>
      </div>
    </section>

    <details class="rag-raw">
      <summary>{copy.rawLabel}</summary>
      <pre data-rag-raw></pre>
    </details>
  </div>
</section>

<script is:inline define:vars={{ copy }} type="module">
  const root = document.querySelector('[data-rag-lab]');

  if (root) {
    const AUTO_MODE = 'auto';
    const FALLBACK_MODES = ['auto', 'fast', 'safe', 'deep_eval', 'creative', 'agentic'];
    const state = {
      bootstrap: null,
      selectedMode: AUTO_MODE,
      manualOverridesEnabled: false,
      traceExpanded: false,
      sourcesExpanded: false,
    };

    const elements = {
      signal: root.querySelector('[data-rag-signal]'),
      status: root.querySelector('[data-rag-status]'),
      statusNote: root.querySelector('[data-rag-status-note]'),
      question: root.querySelector('#rag-question'),
      modes: root.querySelector('[data-rag-modes]'),
      modeHelper: root.querySelector('[data-rag-mode-helper]'),
      modeNote: root.querySelector('[data-rag-mode-note]'),
      manualEnabled: root.querySelector('[data-rag-manual-enabled]'),
      manualFields: Array.from(root.querySelectorAll('[data-rag-field]')),
      manualHelper: root.querySelector('[data-rag-manual-helper]'),
      manualState: root.querySelector('[data-rag-manual-state]'),
      metricHealth: root.querySelector('[data-rag-metric-health]'),
      metricPolicy: root.querySelector('[data-rag-metric-policy]'),
      metricMode: root.querySelector('[data-rag-metric-mode]'),
      metricLatency: root.querySelector('[data-rag-metric-latency]'),
      metricSources: root.querySelector('[data-rag-metric-sources]'),
      answer: root.querySelector('[data-rag-answer]'),
      route: root.querySelector('[data-rag-route]'),
      policy: root.querySelector('[data-rag-policy]'),
      runtime: root.querySelector('[data-rag-runtime]'),
      plan: root.querySelector('[data-rag-plan]'),
      timeline: root.querySelector('[data-rag-timeline]'),
      traceToggle: root.querySelector('[data-rag-trace-toggle]'),
      sources: root.querySelector('[data-rag-sources]'),
      sourcesToggle: root.querySelector('[data-rag-sources-toggle]'),
      raw: root.querySelector('[data-rag-raw]'),
      submit: root.querySelector('[data-rag-submit]'),
      reset: root.querySelector('[data-rag-reset]'),
    };

    const defaultQuestion = copy.examples[0];
    elements.question.value = defaultQuestion;

    const escapeHtml = (value) =>
      String(value ?? '')
        .replaceAll('&', '&amp;')
        .replaceAll('<', '&lt;')
        .replaceAll('>', '&gt;')
        .replaceAll('"', '&quot;')
        .replaceAll("'", '&#39;');

    const formatMaybe = (value) => {
      if (value === null || value === undefined || value === '') return '—';
      if (typeof value === 'boolean') return value ? 'true' : 'false';
      if (typeof value === 'number' && Number.isFinite(value)) return String(value);
      return String(value);
    };

    const formatLatency = (value) => {
      if (typeof value !== 'number' || Number.isNaN(value)) return '—';
      return `${value >= 100 ? value.toFixed(0) : value.toFixed(1)} ms`;
    };

    const getProfileMeta = (mode) => {
      const profiles = state.bootstrap?.queryProfiles || {};
      if (mode === AUTO_MODE) return profiles.auto_mode || null;
      return profiles.profiles?.[mode] || null;
    };

    const getModeDescription = (mode) => getProfileMeta(mode)?.description || '';

    const truncateText = (value, maxLength) => {
      const text = String(value ?? '').replace(/\s+/g, ' ').trim();
      if (!text) return '';
      return text.length > maxLength ? `${text.slice(0, maxLength - 1)}…` : text;
    };

    const stageSummary = (stage) => {
      if (!stage) return '';
      const output = stage.output || {};
      const metadata = stage.metadata || {};
      switch (stage.name) {
        case 'planner':
          return metadata.route_reason || output.rewritten_question || '';
        case 'retrieval':
          return `${output.output_count ?? output.retrieved_count ?? 0} source(s) via ${metadata.retrieval_mode || 'rag'}`;
        case 'parent_expansion':
          return `${output.source_count ?? 0} expanded source(s)`;
        case 'context_compression':
          return metadata.enabled ? `Compressed ${metadata.compressed_sources ?? 0} source(s)` : 'Compression off';
        case 'synthesis':
          return metadata.diagnostics?.deterministic_reason || metadata.model || `Answer chars: ${metadata.answer_chars ?? '—'}`;
        case 'citation_check':
          return `score ${formatMaybe(output.score)} / passed ${formatMaybe(output.passed)}`;
        case 'reflect_retry':
          return `retry decision: ${metadata.retry_decision || output.retry_decision || 'n/a'}`;
        case 'engine.ask':
          return `${output.source_count ?? 0} source(s), ${output.answer_chars ?? 0} chars`;
        default:
          return JSON.stringify(output || metadata).slice(0, 140);
      }
    };

    const routeSummary = (response) => {
      const policy = response.runtime_policy_router || {};
      const resolved = policy.resolved || {};
      const chips = [];
      chips.push(response.query_profile?.mode || resolved.query_mode || state.selectedMode);
      chips.push(response.filters?.retrieval_mode || resolved.retrieval_mode || 'hybrid');
      chips.push((response.workflow?.enabled || resolved.use_workflow) ? 'workflow:on' : 'workflow:off');
      chips.push((response.tool_routing?.enabled || resolved.use_tool_routing) ? `route:${response.tool_routing?.route || resolved.tool_route || 'tool'}` : 'route:rag');
      chips.push((response.retry_loop?.enabled || resolved.use_retry_loop) ? `retry:${response.retry_loop?.retry_attempt_count ?? 0}` : 'retry:off');
      return chips.map((chip) => `<span class="rag-path-chip">${escapeHtml(chip)}</span>`).join('');
    };

    const setStatus = (healthy, runtimePolicyLive) => {
      elements.signal.classList.toggle('is-healthy', healthy);
      elements.signal.classList.toggle('is-bad', !healthy);
      elements.status.textContent = healthy ? copy.statusReady : copy.statusError;
      elements.statusNote.textContent = state.bootstrap?.proxy?.apiBaseUrl || '';
      elements.statusNote.setAttribute('data-policy-live', runtimePolicyLive ? 'true' : 'false');
    };

    const renderModes = () => {
      const queryProfiles = state.bootstrap?.queryProfiles;
      const supportedModes = queryProfiles?.supported_modes || FALLBACK_MODES;
      const profileDescription = getModeDescription(state.selectedMode);
      elements.modes.querySelectorAll('[data-rag-mode]').forEach((button) => {
        const mode = button.getAttribute('data-rag-mode') || '';
        const isSupported = supportedModes.includes(mode);
        button.hidden = !isSupported;
        button.classList.toggle('is-active', mode === state.selectedMode);
      });

      elements.modeHelper.textContent =
        state.selectedMode === AUTO_MODE
          ? copy.modeHintAuto
          : profileDescription || copy.modeHintManual;
      elements.modeNote.textContent = '';
      elements.modeNote.hidden = true;
    };

    const renderMetrics = (payload = null) => {
      const healthy = state.bootstrap?.health?.status === 'ok';
      const runtimePolicyLive = Boolean(state.bootstrap?.capabilities?.runtimePolicyEndpoint);
      const resolvedMode = payload?.query_profile?.mode || state.selectedMode || AUTO_MODE;
      const observedLatency = payload?.runtime_budget?.postrun?.observed?.latency_ms ?? null;
      const sourcesCount = (payload?.sources || []).length;
      elements.metricHealth.textContent = healthy ? 'OK' : copy.disconnected;
      elements.metricPolicy.textContent = runtimePolicyLive ? copy.policyLive : copy.policyFallback;
      elements.metricMode.textContent = resolvedMode === AUTO_MODE ? 'Auto' : resolvedMode;
      elements.metricLatency.textContent = formatLatency(observedLatency);
      elements.metricSources.textContent = String(sourcesCount);
    };

    const updateManualState = () => {
      elements.manualEnabled.checked = state.manualOverridesEnabled;
      elements.manualFields.forEach((field) => {
        field.disabled = !state.manualOverridesEnabled;
      });
      elements.manualHelper.textContent = state.manualOverridesEnabled ? copy.manualHelper : copy.manualDescription;
      elements.manualState.textContent = state.manualOverridesEnabled ? copy.manualToggle : copy.manualSummaryClosed;
    };

    const renderPolicy = (response) => {
      const policy = response.runtime_policy_router || {};
      const resolved = policy.resolved || {};
      const explanation = escapeHtml(policy.user_explanation || policy.reasoning || copy.policyEmpty);
      const badges = [
        resolved.query_mode || response.query_profile?.mode || 'safe',
        resolved.use_workflow ? 'workflow:on' : 'workflow:off',
        resolved.use_tool_routing ? `route:${resolved.tool_route || 'tool'}` : 'route:rag',
        resolved.use_retry_loop ? 'retry:on' : 'retry:off',
      ];

      elements.policy.innerHTML = `
        <article class="rag-policy-card">
          <p class="rag-policy-lead">${explanation}</p>
          <div class="rag-route rag-route-inline">${badges.map((badge) => `<span class="rag-path-chip">${escapeHtml(badge)}</span>`).join('')}</div>
          <div class="rag-policy-meta">
            <div><span>Implementation</span><strong>${escapeHtml(policy.implementation || policy.fallback || '—')}</strong></div>
            <div><span>Confidence</span><strong>${escapeHtml(formatMaybe(policy.confidence))}</strong></div>
            <div><span>Tool route</span><strong>${escapeHtml(resolved.tool_route || response.tool_routing?.route || 'rag_search')}</strong></div>
          </div>
        </article>
      `;
    };

    const renderPlan = (response) => {
      const plan = response.plan || {};
      const originalQuestion = plan.original_question || response.question || '—';
      const rewrittenQuestion = plan.rewritten_question || originalQuestion;
      const routeReason = plan.route_reason || response.runtime_policy_router?.reasoning || '—';
      const rewriteReason = plan.rewrite_reason || response.runtime_policy_router?.user_explanation || '—';
      elements.plan.innerHTML = `
        <article class="rag-mini-card"><span>Original</span><strong>${escapeHtml(originalQuestion)}</strong></article>
        <article class="rag-mini-card"><span>Rewritten</span><strong>${escapeHtml(rewrittenQuestion)}</strong></article>
        <article class="rag-mini-card"><span>Route reason</span><strong>${escapeHtml(routeReason)}</strong></article>
        <article class="rag-mini-card"><span>Rewrite reason</span><strong>${escapeHtml(rewriteReason)}</strong></article>
      `;
    };

    const renderPlanEmpty = () => {
      elements.plan.innerHTML = `
        <article class="rag-mini-card"><span>Original</span><strong>—</strong></article>
        <article class="rag-mini-card"><span>Rewritten</span><strong>—</strong></article>
        <article class="rag-mini-card"><span>Route reason</span><strong>${escapeHtml(copy.plannerEmpty)}</strong></article>
        <article class="rag-mini-card"><span>Rewrite reason</span><strong>—</strong></article>
      `;
    };

    const renderRuntime = (response) => {
      const budget = response.runtime_budget || {};
      const retry = response.retry_loop || {};
      const flags = response.query_profile?.resolved_flags || {};
      elements.runtime.innerHTML = `
        <article class="rag-mini-card"><span>Workflow rail</span><strong>${escapeHtml(response.workflow?.enabled ? 'enabled' : 'engine path')}</strong></article>
        <article class="rag-mini-card"><span>Tool route</span><strong>${escapeHtml(response.tool_routing?.route || 'rag_search')}</strong></article>
        <article class="rag-mini-card"><span>Retry attempts</span><strong>${escapeHtml(String(retry.retry_attempt_count ?? 0))}</strong></article>
        <article class="rag-mini-card"><span>Latency budget</span><strong>${escapeHtml(String(budget.limits?.max_latency_ms ?? '—'))} ms</strong></article>
        <article class="rag-mini-card"><span>Retrieval mode</span><strong>${escapeHtml(flags.retrieval_mode || response.filters?.retrieval_mode || 'hybrid')}</strong></article>
        <article class="rag-mini-card"><span>Min score</span><strong>${escapeHtml(formatMaybe(response.filters?.min_score ?? flags.min_score ?? '—'))}</strong></article>
      `;
    };

    const renderRuntimeEmpty = () => {
      elements.runtime.innerHTML = `
        <article class="rag-mini-card"><span>Workflow rail</span><strong>—</strong></article>
        <article class="rag-mini-card"><span>Tool route</span><strong>—</strong></article>
        <article class="rag-mini-card"><span>Retry attempts</span><strong>—</strong></article>
        <article class="rag-mini-card"><span>Latency budget</span><strong>—</strong></article>
        <article class="rag-mini-card"><span>Retrieval mode</span><strong>—</strong></article>
        <article class="rag-mini-card"><span>Min score</span><strong>${escapeHtml(copy.runtimeEmpty)}</strong></article>
      `;
    };

    const renderTimeline = (response) => {
      const stages = response.observability?.timings?.stages || [];
      if (!stages.length) {
        elements.timeline.textContent = copy.timelineEmpty;
        elements.traceToggle.hidden = true;
        return;
      }

      const previewCount = state.traceExpanded ? stages.length : Math.min(stages.length, 5);
      const previewStages = stages.slice(0, previewCount);
      elements.traceToggle.hidden = stages.length <= 5;
      elements.traceToggle.textContent = state.traceExpanded ? copy.traceCollapse : copy.traceExpand;

      elements.timeline.innerHTML = previewStages
        .map(
          (stage) => `
            <article class="rag-stage-card">
              <div class="rag-stage-marker"></div>
              <div class="rag-stage-body">
                <div class="rag-stage-head">
                  <strong>${escapeHtml(stage.name || 'stage')}</strong>
                  <span>${escapeHtml(formatLatency(stage.latency_ms))}</span>
                </div>
                <p>${escapeHtml(truncateText(stageSummary(stage), 140))}</p>
              </div>
            </article>
          `
        )
        .join('');
    };

    const renderSources = (response) => {
      const sources = response.sources || [];
      if (!sources.length) {
        elements.sources.textContent = copy.sourceEmpty;
        elements.sourcesToggle.hidden = true;
        return;
      }

      const previewCount = state.sourcesExpanded ? sources.length : Math.min(sources.length, 2);
      elements.sourcesToggle.hidden = sources.length <= 2;
      elements.sourcesToggle.textContent = state.sourcesExpanded ? copy.sourcesCollapse : copy.sourcesExpand;

      elements.sources.innerHTML = sources
        .slice(0, previewCount)
        .map(
          (source, index) => `
            <article class="rag-source-card">
              <div class="rag-source-head">
                <strong>${escapeHtml(source.file_name || source.metadata?.file_name || `Source ${index + 1}`)}</strong>
                <span>${escapeHtml(source.marker || source.citation_marker || `#${index + 1}`)}</span>
              </div>
              <p class="rag-source-meta">score ${escapeHtml(formatMaybe(source.score))} · ${escapeHtml(source.metadata?.document_type || source.metadata?.file_type || 'source')}</p>
              <p class="rag-source-preview">${escapeHtml(truncateText(source.preview || source.text || '', 220))}</p>
            </article>
          `
        )
        .join('');
    };

    const renderResponse = (response) => {
      elements.answer.textContent = response.answer || '';
      elements.route.innerHTML = routeSummary(response);
      elements.raw.textContent = JSON.stringify(response, null, 2);
      renderPolicy(response);
      renderPlan(response);
      renderRuntime(response);
      renderTimeline(response);
      renderSources(response);
      renderMetrics(response);
    };

    const renderError = async (response) => {
      const payload = await response.json().catch(() => ({}));
      elements.answer.textContent = `${copy.failedPrefix}: ${payload?.detail?.message || payload?.detail || payload?.error || response.statusText}`;
      elements.route.textContent = copy.routeEmpty;
      elements.policy.textContent = copy.policyEmpty;
      renderPlanEmpty();
      renderRuntimeEmpty();
      elements.timeline.textContent = copy.timelineEmpty;
      elements.traceToggle.hidden = true;
      elements.sources.textContent = copy.sourceEmpty;
      elements.sourcesToggle.hidden = true;
      elements.raw.textContent = JSON.stringify(payload, null, 2);
      renderMetrics();
    };

    const coerceNumber = (value, { min = -Infinity, max = Infinity, integer = false } = {}) => {
      if (value === '' || value === null || value === undefined) return null;
      const numeric = Number(value);
      if (!Number.isFinite(numeric)) return null;
      const candidate = integer ? Math.trunc(numeric) : numeric;
      if (candidate < min || candidate > max) return null;
      return candidate;
    };

    const getPayload = () => {
      const payload = {
        question: elements.question.value.trim(),
        query_mode: state.selectedMode,
        use_runtime_policy_router: state.selectedMode === AUTO_MODE,
      };

      if (state.manualOverridesEnabled) {
        for (const field of elements.manualFields) {
          const key = field.getAttribute('data-rag-field');
          if (!key) continue;

          if (field.type === 'checkbox') {
            payload[key] = Boolean(field.checked);
          } else if (key === 'min_score') {
            const value = coerceNumber(field.value, { min: 0, max: 1 });
            if (value !== null) payload[key] = value;
          } else if (key === 'max_retries') {
            const value = coerceNumber(field.value, { min: 0, max: 5, integer: true });
            if (value !== null) payload[key] = value;
          } else if (field.value) {
            payload[key] = field.value;
          }
        }
      }

      return payload;
    };

    const getRenderedResponse = () => {
      if (!elements.raw.textContent) return null;
      try {
        return JSON.parse(elements.raw.textContent);
      } catch {
        return null;
      }
    };

    const setLoading = (loading) => {
      elements.submit.disabled = loading;
      elements.reset.disabled = loading;
      elements.manualEnabled.disabled = loading;
      elements.manualFields.forEach((field) => {
        field.disabled = loading || !state.manualOverridesEnabled;
      });
      elements.question.disabled = loading;
      elements.submit.textContent = loading ? copy.askingButton : copy.askButton;
    };

    const resetDefaults = () => {
      const defaults = state.bootstrap?.defaults || {};
      state.selectedMode = defaults.query_mode || AUTO_MODE;
      state.manualOverridesEnabled = false;
      state.traceExpanded = false;
      state.sourcesExpanded = false;
      elements.question.value = defaultQuestion;

      for (const field of elements.manualFields) {
        const key = field.getAttribute('data-rag-field');
        if (!key) continue;
        const value = defaults[key];
        if (field.type === 'checkbox') {
          field.checked = Boolean(value);
        } else if (value === null || value === undefined) {
          field.value = '';
        } else {
          field.value = String(value);
        }
      }

      updateManualState();
      renderModes();
      renderMetrics();
      elements.answer.textContent = copy.emptyAnswer;
      elements.route.textContent = copy.routeEmpty;
      elements.policy.textContent = copy.policyEmpty;
      renderPlanEmpty();
      renderRuntimeEmpty();
      elements.timeline.textContent = copy.timelineEmpty;
      elements.traceToggle.hidden = true;
      elements.sources.textContent = copy.sourceEmpty;
      elements.sourcesToggle.hidden = true;
      elements.raw.textContent = '';
    };

    root.querySelectorAll('[data-rag-example]').forEach((button) => {
      button.addEventListener('click', () => {
        elements.question.value = button.getAttribute('data-rag-example') || '';
        elements.question.focus();
      });
    });

    elements.modes.querySelectorAll('[data-rag-mode]').forEach((button) => {
      button.addEventListener('click', () => {
        const nextMode = button.getAttribute('data-rag-mode') || AUTO_MODE;
        state.selectedMode = nextMode;
        renderModes();
        renderMetrics();
      });
    });

    elements.manualEnabled.addEventListener('change', () => {
      state.manualOverridesEnabled = elements.manualEnabled.checked;
      updateManualState();
    });

    elements.traceToggle.addEventListener('click', () => {
      state.traceExpanded = !state.traceExpanded;
      const payload = getRenderedResponse();
      if (payload) renderTimeline(payload);
    });

    elements.sourcesToggle.addEventListener('click', () => {
      state.sourcesExpanded = !state.sourcesExpanded;
      const payload = getRenderedResponse();
      if (payload) renderSources(payload);
    });

    elements.reset.addEventListener('click', resetDefaults);

    elements.submit.addEventListener('click', async () => {
      const payload = getPayload();
      if (!payload.question) {
        elements.answer.textContent = copy.needsQuestion;
        return;
      }

      setLoading(true);
      state.traceExpanded = false;
      state.sourcesExpanded = false;
      elements.answer.textContent = copy.runningAnswer;
      elements.route.textContent = copy.routeEmpty;
      elements.policy.textContent = copy.policyEmpty;
      renderPlanEmpty();
      renderRuntimeEmpty();
      elements.timeline.textContent = copy.timelineEmpty;
      elements.traceToggle.hidden = true;
      elements.sources.textContent = copy.sourceEmpty;
      elements.sourcesToggle.hidden = true;
      elements.raw.textContent = '';

      try {
        const response = await fetch('/api/rag/ask', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify(payload),
        });

        if (!response.ok) {
          await renderError(response);
        } else {
          renderResponse(await response.json());
        }
      } catch (error) {
        elements.answer.textContent = `${copy.failedPrefix}: ${String(error)}`;
      } finally {
        setLoading(false);
      }
    });

    try {
      const bootstrapResponse = await fetch('/api/rag/bootstrap');
      const bootstrapData = await bootstrapResponse.json();
      if (!bootstrapResponse.ok || !bootstrapData.ok) {
        throw new Error(bootstrapData.error || 'Failed to load bootstrap data');
      }
      state.bootstrap = bootstrapData;
      resetDefaults();
      setStatus(state.bootstrap?.health?.status === 'ok', Boolean(state.bootstrap?.capabilities?.runtimePolicyEndpoint));
      renderModes();
      renderMetrics();
    } catch (error) {
      elements.signal.classList.add('is-bad');
      elements.status.textContent = copy.statusError;
      elements.statusNote.textContent = String(error);
      renderPlanEmpty();
      renderRuntimeEmpty();
    }
  }
</script>

<style is:global>
  .rag-lab {
    margin: var(--space-5) 0 var(--space-6);
  }

  .rag-shell {
    position: relative;
    overflow: hidden;
    border-radius: 36px;
    padding: clamp(22px, 3vw, 34px);
    border: 1px solid color-mix(in srgb, var(--color-accent) 18%, var(--color-border));
    background:
      radial-gradient(circle at 8% 8%, rgba(201, 121, 45, 0.16), transparent 28%),
      radial-gradient(circle at 88% 14%, rgba(17, 118, 122, 0.12), transparent 30%),
      linear-gradient(180deg, rgba(255, 251, 245, 0.98), rgba(247, 241, 232, 0.92));
    box-shadow:
      0 26px 90px rgba(43, 29, 16, 0.12),
      inset 0 1px 0 rgba(255, 255, 255, 0.7);
  }

  .rag-atmosphere {
    position: absolute;
    border-radius: 999px;
    filter: blur(24px);
    pointer-events: none;
    opacity: 0.55;
  }

  .rag-atmosphere-a {
    width: 260px;
    height: 260px;
    top: -80px;
    right: -90px;
    background: radial-gradient(circle, rgba(196, 122, 50, 0.3), transparent 68%);
  }

  .rag-atmosphere-b {
    width: 220px;
    height: 220px;
    left: -60px;
    bottom: -80px;
    background: radial-gradient(circle, rgba(29, 105, 110, 0.15), transparent 70%);
  }

  .rag-hero,
  .rag-telemetry-ribbon,
  .rag-workbench-grid,
  .rag-output-grid,
  .rag-raw {
    position: relative;
    z-index: 1;
  }

  .rag-hero {
    display: grid;
    grid-template-columns: minmax(0, 1.12fr) minmax(300px, 0.88fr);
    gap: 28px;
    align-items: center;
    margin-bottom: 24px;
  }

  .rag-eyebrow,
  .rag-kicker {
    margin: 0 0 8px;
    color: color-mix(in srgb, var(--color-accent) 74%, #7a4b1d);
    font-family: var(--font-mono);
    font-size: 0.74rem;
    letter-spacing: 0.16em;
    text-transform: uppercase;
  }

  .rag-title {
    margin: 0;
    max-width: 8.8ch;
    color: #20150e;
    font-size: clamp(2.6rem, 5.2vw, 5.5rem);
    line-height: 0.92;
    letter-spacing: -0.05em;
    text-wrap: balance;
  }

  .rag-description {
    margin-top: 16px;
    max-width: 64ch;
    color: rgba(32, 21, 14, 0.74);
    font-size: 1rem;
    line-height: 1.72;
  }

  .rag-runtime-plaque,
  .rag-composer-card,
  .rag-mode-card,
  .rag-advanced,
  .rag-panel,
  .rag-raw {
    border: 1px solid rgba(96, 68, 45, 0.12);
    background: rgba(255, 252, 248, 0.75);
    backdrop-filter: blur(12px);
    box-shadow:
      0 16px 44px rgba(75, 48, 25, 0.08),
      inset 0 1px 0 rgba(255, 255, 255, 0.68);
  }

  .rag-runtime-plaque {
    justify-self: end;
    width: min(100%, 360px);
    border-radius: 28px;
    padding: 18px 20px;
  }

  .rag-runtime-head {
    display: flex;
    align-items: center;
    gap: 14px;
  }

  .rag-signal-dot {
    width: 12px;
    height: 12px;
    flex: none;
    border-radius: 999px;
    background: #b8a592;
    box-shadow: 0 0 0 8px rgba(184, 165, 146, 0.16);
  }

  .rag-signal-dot.is-healthy {
    background: #1f8d72;
    box-shadow: 0 0 0 8px rgba(31, 141, 114, 0.16);
  }

  .rag-signal-dot.is-bad {
    background: #cb5b58;
    box-shadow: 0 0 0 8px rgba(203, 91, 88, 0.16);
  }

  .rag-runtime-label {
    margin: 0;
    color: #20150e;
    font-family: var(--font-display);
    font-weight: 600;
  }

  .rag-runtime-caption,
  .rag-runtime-endpoint {
    margin: 6px 0 0;
    color: rgba(32, 21, 14, 0.64);
    font-size: 0.86rem;
  }

  .rag-runtime-endpoint {
    margin-top: 14px;
    font-family: var(--font-mono);
    font-size: 0.78rem;
    word-break: break-all;
  }

  .rag-telemetry-ribbon {
    display: grid;
    grid-template-columns: repeat(5, minmax(0, 1fr));
    align-items: stretch;
    gap: 0;
    margin-bottom: 22px;
    border-radius: 22px;
    overflow: hidden;
    border: 1px solid rgba(96, 68, 45, 0.11);
    background: rgba(255, 250, 244, 0.58);
    backdrop-filter: blur(10px);
    box-shadow:
      0 14px 34px rgba(75, 48, 25, 0.06),
      inset 0 1px 0 rgba(255,255,255,0.62);
  }

  .rag-metric-card {
    display: grid;
    gap: 6px;
    min-width: 0;
    padding: 16px 18px;
    border-right: 1px solid rgba(96, 68, 45, 0.08);
    background: transparent;
  }

  .rag-metric-card:last-child {
    border-right: 0;
  }

  .rag-metric-card span {
    color: rgba(32, 21, 14, 0.54);
    font-family: var(--font-mono);
    font-size: 0.72rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .rag-metric-card strong {
    color: #20150e;
    font-size: 0.96rem;
    font-weight: 700;
  }

  .rag-composer-card {
    border-radius: 32px;
    padding: 22px;
  }

  .rag-workbench-grid {
    display: grid;
    grid-template-columns: minmax(340px, 0.82fr) minmax(0, 1.18fr);
    gap: 18px;
    align-items: start;
  }

  .rag-left-rail,
  .rag-right-stage {
    display: grid;
    gap: 18px;
    align-content: start;
  }

  .rag-composer-head,
  .rag-panel-head,
  .rag-mode-head {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 14px;
    margin-bottom: 14px;
  }

  .rag-section-title {
    margin: 0;
    color: #20150e;
    font-size: 1.12rem;
  }

  .rag-field-shell {
    display: block;
    border-radius: 24px;
    padding: 2px;
    background: linear-gradient(180deg, rgba(255,255,255,0.7), rgba(244,232,219,0.9));
  }

  .rag-question,
  .rag-select,
  .rag-number {
    width: 100%;
    border: 1px solid rgba(95, 67, 44, 0.14);
    background: rgba(255, 255, 255, 0.82);
    color: #20150e;
    font: inherit;
  }

  .rag-question {
    min-height: 154px;
    border-radius: 22px;
    padding: 18px 20px;
    resize: vertical;
    line-height: 1.65;
    box-shadow: inset 0 1px 0 rgba(255,255,255,0.7);
  }

  .rag-question:focus,
  .rag-select:focus,
  .rag-number:focus {
    outline: none;
    border-color: rgba(201, 121, 45, 0.45);
    box-shadow: 0 0 0 4px rgba(201, 121, 45, 0.12);
  }

  .rag-composer-actions {
    display: flex;
    align-items: center;
    gap: 12px;
    margin-top: 14px;
  }

  .rag-primary,
  .rag-secondary,
  .rag-chip,
  .rag-mode-pill,
  .rag-select,
  .rag-number {
    font: inherit;
  }

  .rag-primary,
  .rag-secondary,
  .rag-chip,
  .rag-mode-pill,
  .rag-advanced-summary {
    transition:
      transform var(--transition-fast),
      border-color var(--transition-fast),
      background var(--transition-fast),
      box-shadow var(--transition-fast),
      color var(--transition-fast);
  }

  .rag-primary {
    border: 0;
    border-radius: 999px;
    padding: 14px 20px;
    background: linear-gradient(135deg, #c9771c, #df9b43);
    color: #fffaf3;
    font-family: var(--font-display);
    font-weight: 700;
    cursor: pointer;
    box-shadow: 0 14px 32px rgba(186, 110, 26, 0.24);
  }

  .rag-primary:hover {
    transform: translateY(-1px);
    box-shadow: 0 18px 36px rgba(186, 110, 26, 0.28);
  }

  .rag-secondary {
    border: 1px solid rgba(95, 67, 44, 0.14);
    border-radius: 999px;
    padding: 14px 18px;
    background: rgba(255,255,255,0.72);
    color: rgba(32, 21, 14, 0.78);
    cursor: pointer;
  }

  .rag-secondary:hover {
    transform: translateY(-1px);
    border-color: rgba(201, 121, 45, 0.28);
  }

  .rag-example-block {
    margin-top: 16px;
  }

  .rag-chip-row {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
  }

  .rag-chip {
    border: 1px solid rgba(95, 67, 44, 0.12);
    background: rgba(255,255,255,0.7);
    color: rgba(32, 21, 14, 0.82);
    padding: 10px 14px;
    border-radius: 999px;
    cursor: pointer;
    font-size: 0.86rem;
  }

  .rag-chip:hover {
    transform: translateY(-1px);
    border-color: rgba(201, 121, 45, 0.26);
  }

  .rag-control-rail {
    display: grid;
    gap: 16px;
  }

  .rag-mode-card,
  .rag-advanced {
    border-radius: 28px;
    padding: 18px 20px;
  }

  .rag-mode-pills {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
  }

  .rag-mode-pill {
    border: 1px solid rgba(95, 67, 44, 0.12);
    border-radius: 999px;
    padding: 10px 16px;
    background: rgba(255,255,255,0.66);
    color: rgba(32, 21, 14, 0.74);
    cursor: pointer;
  }

  .rag-mode-pill.is-active {
    border-color: rgba(182, 111, 34, 0.38);
    background: linear-gradient(135deg, rgba(184, 114, 37, 0.94), rgba(222, 162, 88, 0.92));
    color: #fff8ef;
    box-shadow: 0 12px 28px rgba(176, 110, 40, 0.22);
  }

  .rag-mode-helper,
  .rag-mode-note,
  .rag-callout,
  .rag-policy-lead,
  .rag-answer,
  .rag-route,
  .rag-source-card p,
  .rag-stage-card p {
    color: rgba(32, 21, 14, 0.72);
    line-height: 1.68;
  }

  .rag-mode-helper,
  .rag-mode-note,
  .rag-callout {
    margin: 14px 0 0;
    font-size: 0.92rem;
  }

  .rag-advanced {
    padding: 0;
    overflow: hidden;
  }

  .rag-advanced[open] .rag-advanced-chevron {
    transform: rotate(180deg);
  }

  .rag-advanced-summary {
    list-style: none;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 16px;
    padding: 18px 20px;
    cursor: pointer;
  }

  .rag-advanced-summary::-webkit-details-marker {
    display: none;
  }

  .rag-advanced-meta {
    display: flex;
    align-items: center;
    gap: 14px;
  }

  .rag-advanced-preview {
    max-width: 28ch;
    color: rgba(32, 21, 14, 0.58);
    font-size: 0.86rem;
    text-align: right;
  }

  .rag-advanced-chevron {
    width: 12px;
    height: 12px;
    border-right: 1.8px solid rgba(32, 21, 14, 0.6);
    border-bottom: 1.8px solid rgba(32, 21, 14, 0.6);
    transform: rotate(45deg);
    transform-origin: center;
  }

  .rag-advanced-body {
    padding: 0 20px 20px;
  }

  .rag-advanced-toggle-row {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 16px;
  }

  .rag-advanced-copy {
    margin: 0;
    color: rgba(32, 21, 14, 0.66);
    font-size: 0.92rem;
    line-height: 1.6;
  }

  .rag-callout {
    padding: 12px 14px;
    border-radius: 18px;
    background: rgba(255, 246, 234, 0.82);
    border: 1px solid rgba(201, 121, 45, 0.14);
  }

  .rag-manual-grid {
    display: grid;
    gap: 12px;
    margin-top: 16px;
  }

  .rag-manual-row,
  .rag-mini-card,
  .rag-source-card,
  .rag-policy-card {
    border: 1px solid rgba(95, 67, 44, 0.12);
    background: rgba(255, 255, 255, 0.72);
    border-radius: 20px;
    box-shadow: inset 0 1px 0 rgba(255,255,255,0.7);
  }

  .rag-manual-row {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    gap: 14px;
    align-items: center;
    padding: 14px;
  }

  .rag-manual-row h4 {
    margin: 0 0 6px;
    color: #20150e;
    font-size: 0.98rem;
  }

  .rag-manual-row p {
    margin: 0;
    color: rgba(32, 21, 14, 0.64);
    font-size: 0.84rem;
    line-height: 1.55;
  }

  .rag-switch {
    display: inline-flex;
    align-items: center;
    gap: 10px;
  }

  .rag-switch input {
    position: absolute;
    opacity: 0;
    pointer-events: none;
  }

  .rag-switch-track {
    position: relative;
    width: 52px;
    height: 30px;
    border-radius: 999px;
    background: rgba(120, 96, 74, 0.22);
    border: 1px solid rgba(95, 67, 44, 0.16);
  }

  .rag-switch-track::after {
    content: '';
    position: absolute;
    top: 3px;
    left: 4px;
    width: 22px;
    height: 22px;
    border-radius: 999px;
    background: rgba(255,255,255,0.94);
    box-shadow: 0 4px 10px rgba(32, 21, 14, 0.12);
    transition: transform var(--transition-fast), background var(--transition-fast);
  }

  .rag-switch input:checked + .rag-switch-track {
    background: rgba(196, 121, 45, 0.4);
    border-color: rgba(182, 111, 34, 0.34);
  }

  .rag-switch input:checked + .rag-switch-track::after {
    transform: translateX(21px);
    background: #fff8f0;
  }

  .rag-switch-label {
    color: rgba(32, 21, 14, 0.72);
    font-size: 0.86rem;
  }

  .rag-select,
  .rag-number {
    max-width: 148px;
    border-radius: 14px;
    padding: 10px 12px;
  }

  .rag-output-grid {
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(280px, 0.86fr);
    grid-template-areas:
      "answer answer"
      "journey journey"
      "policy runtime"
      "planner trace"
      "planner sources";
    gap: 18px;
  }

  .rag-panel {
    border-radius: 30px;
    padding: 20px;
  }

  .rag-panel-answer { grid-area: answer; }
  .rag-panel-journey { grid-area: journey; }
  .rag-panel-policy { grid-area: policy; }
  .rag-panel-runtime { grid-area: runtime; }
  .rag-panel-planner { grid-area: planner; }
  .rag-panel-trace { grid-area: trace; }
  .rag-panel-sources { grid-area: sources; }

  .rag-panel-policy,
  .rag-panel-runtime,
  .rag-panel-planner,
  .rag-panel-trace,
  .rag-panel-sources {
    min-height: 296px;
  }

  .rag-answer {
    white-space: pre-wrap;
    font-size: 0.98rem;
    min-height: 110px;
  }

  .rag-route {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
  }

  .rag-route-inline {
    margin-top: 14px;
  }

  .rag-path-chip {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    border-radius: 999px;
    padding: 10px 14px;
    background: rgba(255,255,255,0.78);
    border: 1px solid rgba(95, 67, 44, 0.12);
    color: rgba(32, 21, 14, 0.76);
    font-family: var(--font-mono);
    font-size: 0.8rem;
  }

  .rag-policy-card {
    padding: 16px;
  }

  .rag-policy-lead {
    margin: 0;
  }

  .rag-policy-meta {
    display: grid;
    gap: 10px;
    margin-top: 16px;
  }

  .rag-policy-meta div,
  .rag-mini-card {
    display: grid;
    gap: 4px;
  }

  .rag-policy-meta span,
  .rag-mini-card span,
  .rag-source-meta,
  .rag-stage-head span {
    color: rgba(32, 21, 14, 0.54);
    font-family: var(--font-mono);
    font-size: 0.74rem;
    letter-spacing: 0.04em;
    text-transform: uppercase;
  }

  .rag-policy-meta strong,
  .rag-mini-card strong,
  .rag-source-head strong,
  .rag-stage-head strong {
    color: #20150e;
    font-weight: 700;
  }

  .rag-mini-grid {
    display: grid;
    gap: 12px;
    align-content: start;
  }

  .rag-mini-card {
    padding: 14px;
  }

  .rag-trace {
    display: grid;
    gap: 14px;
    align-content: start;
  }

  .rag-stage-card {
    position: relative;
    display: grid;
    grid-template-columns: 18px minmax(0, 1fr);
    gap: 14px;
  }

  .rag-stage-marker {
    position: relative;
    width: 18px;
  }

  .rag-stage-marker::before {
    content: '';
    position: absolute;
    top: 2px;
    left: 5px;
    width: 9px;
    height: 9px;
    border-radius: 999px;
    background: linear-gradient(135deg, rgba(201, 121, 45, 0.95), rgba(230, 171, 95, 0.92));
    box-shadow: 0 0 0 5px rgba(201, 121, 45, 0.12);
  }

  .rag-stage-marker::after {
    content: '';
    position: absolute;
    top: 18px;
    bottom: -12px;
    left: 8px;
    width: 1px;
    background: linear-gradient(180deg, rgba(201,121,45,0.22), transparent);
  }

  .rag-stage-card:last-child .rag-stage-marker::after {
    display: none;
  }

  .rag-stage-head,
  .rag-source-head {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 12px;
    margin-bottom: 6px;
  }

  .rag-stage-body p,
  .rag-source-preview {
    margin: 0;
    font-size: 0.92rem;
  }

  .rag-sources {
    display: grid;
    gap: 12px;
    align-content: start;
  }

  .rag-source-card {
    padding: 14px;
  }

  .rag-source-meta {
    margin: 0 0 8px;
  }

  .rag-raw {
    margin-top: 18px;
    border-radius: 24px;
    overflow: hidden;
  }

  .rag-raw summary {
    cursor: pointer;
    padding: 16px 18px;
    color: rgba(32, 21, 14, 0.72);
    font-family: var(--font-display);
    font-weight: 600;
  }

  .rag-raw pre {
    margin: 0;
    border: 0;
    background: transparent;
    color: rgba(32, 21, 14, 0.8);
    padding: 0 18px 18px;
    max-height: 420px;
    overflow: auto;
  }

  .rag-link-button {
    border: 0;
    padding: 0;
    background: transparent;
    color: color-mix(in srgb, var(--color-accent) 74%, #7a4b1d);
    font-family: var(--font-mono);
    font-size: 0.74rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    cursor: pointer;
  }

  .rag-link-button:hover {
    color: #a4601c;
  }

  :global(html[data-theme='dark']) .rag-shell {
    border-color: rgba(196, 140, 72, 0.18);
    background:
      radial-gradient(circle at 10% 10%, rgba(206, 132, 56, 0.14), transparent 26%),
      radial-gradient(circle at 88% 14%, rgba(41, 111, 112, 0.12), transparent 28%),
      linear-gradient(180deg, rgba(20, 17, 14, 0.98), rgba(13, 11, 10, 0.96));
    box-shadow:
      0 28px 100px rgba(0, 0, 0, 0.42),
      inset 0 1px 0 rgba(255,255,255,0.04);
  }

  :global(html[data-theme='dark']) .rag-runtime-plaque,
  :global(html[data-theme='dark']) .rag-composer-card,
  :global(html[data-theme='dark']) .rag-mode-card,
  :global(html[data-theme='dark']) .rag-advanced,
  :global(html[data-theme='dark']) .rag-panel,
  :global(html[data-theme='dark']) .rag-raw {
    border-color: rgba(157, 118, 72, 0.16);
    background: rgba(24, 20, 17, 0.82);
    box-shadow:
      0 18px 40px rgba(0,0,0,0.32),
      inset 0 1px 0 rgba(255,255,255,0.03);
  }

  :global(html[data-theme='dark']) .rag-title,
  :global(html[data-theme='dark']) .rag-runtime-label,
  :global(html[data-theme='dark']) .rag-section-title,
  :global(html[data-theme='dark']) .rag-manual-row h4,
  :global(html[data-theme='dark']) .rag-policy-meta strong,
  :global(html[data-theme='dark']) .rag-mini-card strong,
  :global(html[data-theme='dark']) .rag-source-head strong,
  :global(html[data-theme='dark']) .rag-stage-head strong,
  :global(html[data-theme='dark']) .rag-metric-card strong {
    color: #f1e6d8;
  }

  :global(html[data-theme='dark']) .rag-description,
  :global(html[data-theme='dark']) .rag-runtime-caption,
  :global(html[data-theme='dark']) .rag-runtime-endpoint,
  :global(html[data-theme='dark']) .rag-mode-helper,
  :global(html[data-theme='dark']) .rag-mode-note,
  :global(html[data-theme='dark']) .rag-callout,
  :global(html[data-theme='dark']) .rag-policy-lead,
  :global(html[data-theme='dark']) .rag-answer,
  :global(html[data-theme='dark']) .rag-route,
  :global(html[data-theme='dark']) .rag-source-card p,
  :global(html[data-theme='dark']) .rag-stage-card p,
  :global(html[data-theme='dark']) .rag-advanced-copy,
  :global(html[data-theme='dark']) .rag-manual-row p,
  :global(html[data-theme='dark']) .rag-switch-label,
  :global(html[data-theme='dark']) .rag-advanced-preview,
  :global(html[data-theme='dark']) .rag-raw summary,
  :global(html[data-theme='dark']) .rag-raw pre {
    color: rgba(241, 230, 216, 0.72);
  }

  :global(html[data-theme='dark']) .rag-field-shell {
    background: linear-gradient(180deg, rgba(255,255,255,0.05), rgba(152,112,66,0.08));
  }

  :global(html[data-theme='dark']) .rag-question,
  :global(html[data-theme='dark']) .rag-select,
  :global(html[data-theme='dark']) .rag-number,
  :global(html[data-theme='dark']) .rag-chip,
  :global(html[data-theme='dark']) .rag-manual-row,
  :global(html[data-theme='dark']) .rag-mini-card,
  :global(html[data-theme='dark']) .rag-source-card,
  :global(html[data-theme='dark']) .rag-policy-card,
  :global(html[data-theme='dark']) .rag-path-chip,
  :global(html[data-theme='dark']) .rag-secondary,
  :global(html[data-theme='dark']) .rag-mode-pill {
    border-color: rgba(157, 118, 72, 0.16);
    background: rgba(31, 25, 21, 0.82);
    color: rgba(241, 230, 216, 0.8);
  }

  :global(html[data-theme='dark']) .rag-telemetry-ribbon {
    border-color: rgba(157, 118, 72, 0.16);
    background: rgba(24, 20, 17, 0.7);
    box-shadow:
      0 18px 36px rgba(0,0,0,0.24),
      inset 0 1px 0 rgba(255,255,255,0.03);
  }

  :global(html[data-theme='dark']) .rag-metric-card {
    border-right-color: rgba(157, 118, 72, 0.12);
  }

  :global(html[data-theme='dark']) .rag-mode-pill.is-active {
    border-color: rgba(224, 169, 98, 0.28);
    background: linear-gradient(135deg, rgba(173, 111, 42, 0.92), rgba(211, 158, 88, 0.9));
    color: #fff8ef;
  }

  :global(html[data-theme='dark']) .rag-primary {
    box-shadow: 0 16px 34px rgba(176, 111, 43, 0.22);
  }

  :global(html[data-theme='dark']) .rag-callout {
    background: rgba(61, 43, 24, 0.36);
    border-color: rgba(170, 121, 68, 0.18);
  }

  :global(html[data-theme='dark']) .rag-policy-meta span,
  :global(html[data-theme='dark']) .rag-mini-card span,
  :global(html[data-theme='dark']) .rag-source-meta,
  :global(html[data-theme='dark']) .rag-stage-head span,
  :global(html[data-theme='dark']) .rag-metric-card span {
    color: rgba(241, 230, 216, 0.5);
  }

  :global(html[data-theme='dark']) .rag-link-button {
    color: rgba(232, 181, 113, 0.9);
  }

  @media (max-width: 1080px) {
    .rag-hero,
    .rag-workbench-grid,
    .rag-output-grid {
      grid-template-columns: 1fr;
    }

    .rag-hero {
      align-items: start;
    }

    .rag-output-grid {
      grid-template-areas:
        "answer"
        "journey"
        "policy"
        "runtime"
        "planner"
        "trace"
        "sources";
    }

    .rag-runtime-plaque {
      justify-self: stretch;
    }

    .rag-telemetry-ribbon {
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 0;
    }

    .rag-metric-card:nth-child(2n) {
      border-right: 0;
    }
  }

  @media (max-width: 720px) {
    .rag-shell {
      border-radius: 28px;
      padding: 18px;
    }

    .rag-title {
      max-width: 8.5ch;
      font-size: clamp(2.4rem, 12vw, 4.4rem);
    }

    .rag-composer-actions {
      flex-wrap: wrap;
    }

    .rag-primary,
    .rag-secondary {
      width: 100%;
      justify-content: center;
    }

    .rag-telemetry-ribbon {
      grid-template-columns: 1fr;
    }

    .rag-metric-card {
      border-right: 0;
      border-bottom: 1px solid rgba(96, 68, 45, 0.08);
    }

    .rag-metric-card:last-child {
      border-bottom: 0;
    }

    .rag-advanced-summary,
    .rag-advanced-toggle-row,
    .rag-manual-row,
    .rag-stage-head,
    .rag-source-head {
      gap: 10px;
    }

    .rag-advanced-summary,
    .rag-advanced-toggle-row,
    .rag-manual-row {
      grid-template-columns: 1fr;
    }

    .rag-advanced-toggle-row,
    .rag-manual-row {
      display: grid;
    }

    .rag-advanced-meta {
      justify-content: space-between;
    }

    .rag-advanced-preview {
      text-align: left;
    }
  }
</style>
