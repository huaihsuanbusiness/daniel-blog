---
export const prerender = true;
export function getStaticPaths() {
  return [{ params: { lang: 'en' } }, { params: { lang: 'zh' } }];
}
import SurveyLayout from '../../../../layouts/SurveyLayout.astro';
---

<SurveyLayout title="感謝填寫" description="">
  <div class="thankyou-block">

    <div class="big-check">✓</div>
    <h2>謝謝您的填寫！</h2>

    <!-- 填寫後的感謝語 -->
    <p id="caregiver-msg" class="thank-msg" style="display:none">
      感謝您分享這麼寶貴的意見。<br />
      您的回覆將幫助我們打造更適合長輩與家屬的 AI 陪伴服務。
    </p>
    <p id="elder-msg" class="thank-msg" style="display:none">
      感謝您撥冗填寫！<br />
      您的回覆對我們非常寶貴。
    </p>

    <!-- 單一分享容器：Prompt + 連結 + 按鈕 -->
    <div id="share-panel" class="share-panel" style="display:none">

      <!-- 邀請提示語（置換後的正確版本） -->
      <p id="elder-prompt" class="prompt-line" style="display:none">
        邀請您分享給家人或照顧者填寫
      </p>
      <p id="caregiver-prompt" class="prompt-line" style="display:none">
        邀請您分享給長輩填寫
      </p>

      <!-- 連結滿寬 -->
      <div class="link-block">
        <div class="share-link" id="share-link"></div>
      </div>

      <!-- 按鈕組：上下排列 -->
      <div class="btn-group">
        <button class="btn-copy" id="btn-copy">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"><rect x="9" y="9" width="13" height="13" rx="2" ry="2"></rect><path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"></path></svg>
          <span id="copy-label">複製</span>
        </button>
        <a class="btn-line" id="btn-line" href="#" target="_blank" rel="noopener">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor"><circle cx="12" cy="12" r="11" fill="#00B900"/><path d="M8.5 7.5h1.75c2.233 0 3.268.93 3.268 2.766 0 1.25-.65 2.109-1.767 2.44l-.033.014c.002.032.002.065.002.097 0 1.58-1.105 2.683-2.98 2.683H8.5v-7.5l.5-.5zm-.75 1.5v2h1.6c1.2 0 1.9-.55 1.9-1.45 0-.9-.7-1.55-1.9-1.55H7.75v4.5zm5 0h-1.75v-.5c0-1.4-.85-2.15-2.1-2.15-.8 0-1.3.4-1.3.9v4h-1.75v-7.75h1.75v.75c.5-.55 1.2-.8 2-.8 1.55 0 2.85 1 2.85 3.25V9h-.75v1.5z" fill="white"/></svg>
          分享到LINE
        </a>
      </div>

      <!-- 提示文字 -->
      <p id="elder-hint" class="share-hint" style="display:none"></p>
      <p id="caregiver-hint" class="share-hint" style="display:none"></p>
    </div>

    <div class="back-row">
      <a href="https://www.danielcanfly.com/zh/blog/ai-companion-survey?category=business" class="btn-back">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"><polyline points="15 18 9 12 15 6"></polyline></svg>
        返回文章
      </a>
    </div>

  </div>
</SurveyLayout>

<style>
  .thankyou-block {
    text-align: center;
    padding: 48px 24px 40px;
    max-width: 520px;
    margin: 0 auto;
  }

  .big-check {
    font-size: 60px;
    line-height: 1;
    margin-bottom: 14px;
  }

  h2 {
    font-size: 1.45rem;
    margin-bottom: 10px;
    color: #fff;
    font-weight: 700;
  }

  .thank-msg {
    font-size: 0.92rem;
    color: #e8e8f0;
    line-height: 1.75;
    margin: 0 0 4px;
  }

  /* 單一分享容器 */
  .share-panel {
    margin-top: 28px;
    background: #f7f8ff;
    border: 1.5px solid #dde3ff;
    border-radius: 14px;
    padding: 18px 20px 14px;
    text-align: left;
  }

  .prompt-line {
    font-size: 0.88rem;
    font-weight: 600;
    color: #111;
    margin: 0 0 12px;
    line-height: 1.5;
  }

  .link-block {
    flex: none;
    width: 100%;
  }
  .share-link {
    width: 100%;
    background: #fff;
    border: 1.5px solid #c5cae9;
    border-radius: 8px;
    padding: 9px 12px;
    font-size: 0.78rem;
    font-family: 'SF Mono', 'Cascadia Code', monospace;
    color: #4f46e5;
    word-break: break-all;
    cursor: pointer;
    user-select: all;
    min-height: 40px;
    display: flex;
    align-items: center;
    box-sizing: border-box;
    transition: border-color 0.15s;
  }
  .share-link:hover { border-color: #4f46e5; }

  .btn-group {
    display: flex;
    gap: 8px;
    flex-shrink: 0;
    margin-top: 8px;
  }

  .btn-copy {
    display: flex;
    align-items: center;
    gap: 4px;
    padding: 0 14px;
    height: 40px;
    background: #4f46e5;
    color: #fff;
    border: none;
    border-radius: 8px;
    font-size: 0.82rem;
    font-weight: 600;
    cursor: pointer;
    white-space: nowrap;
    flex: 1;
    transition: background 0.15s;
  }
  .btn-copy:hover { background: #4338ca; }
  .btn-copy.copied { background: #16a34a; }

  .btn-line {
    display: flex;
    align-items: center;
    gap: 4px;
    height: 40px;
    padding: 0 14px;
    background: #00B900;
    color: #fff;
    border-radius: 8px;
    font-size: 0.82rem;
    font-weight: 700;
    text-decoration: none;
    white-space: nowrap;
    flex: 1;
    transition: background 0.15s;
  }
  .btn-line:hover { background: #009100; }

  .share-hint {
    font-size: 0.78rem;
    color: #b0b0c8;
    margin: 8px 0 0;
    line-height: 1.5;
  }

  .back-row {
    margin-top: 28px;
    text-align: center;
  }

  .btn-back {
    display: inline-flex;
    align-items: center;
    gap: 5px;
    padding: 10px 22px;
    color: #555;
    text-decoration: none;
    font-size: 0.88rem;
    border: 1.5px solid #ddd;
    border-radius: 8px;
    transition: all 0.15s;
  }
  .btn-back:hover {
    color: #4f46e5;
    border-color: #4f46e5;
  }
</style>

<script>
  const urlParams = new URLSearchParams(window.location.search);
  const role = urlParams.get('role') || sessionStorage.getItem('survey_role') || '';
  const inviteUrl = urlParams.get('invite') || '';

  const panel = document.getElementById('share-panel');
  const linkEl = document.getElementById('share-link');
  const btnCopy = document.getElementById('btn-copy');
  const copyLabel = document.getElementById('copy-label');
  const btnLine = document.getElementById('btn-line');

  if (role === 'caregiver') {
    document.getElementById('caregiver-msg').style.display = 'block';
    document.getElementById('caregiver-prompt').style.display = 'block';
    document.getElementById('caregiver-hint').style.display = 'block';
    if (inviteUrl) {
      panel.style.display = 'block';
      linkEl.textContent = inviteUrl;
    }
  } else if (role === 'elder') {
    document.getElementById('elder-msg').style.display = 'block';
    document.getElementById('elder-prompt').style.display = 'block';
    document.getElementById('elder-hint').style.display = 'block';
    if (inviteUrl) {
      panel.style.display = 'block';
      linkEl.textContent = inviteUrl;
    }
  }

  // 複製按鈕：停在「已複製 ✓」
  if (btnCopy && inviteUrl) {
    btnCopy.addEventListener('click', () => {
      navigator.clipboard.writeText(inviteUrl).then(() => {
        copyLabel.textContent = '已複製 ✓';
        btnCopy.classList.add('copied');
      });
    });
  }

  // LINE 分享
  if (btnLine && inviteUrl) {
    btnLine.href = 'https://social-plugins.line.me/lineit/share?url=' + encodeURIComponent(inviteUrl);
  }

  // 清理 sessionStorage
  sessionStorage.removeItem('survey_familyId');
  sessionStorage.removeItem('survey_inviteLink');
  sessionStorage.removeItem('survey_role');
</script>
